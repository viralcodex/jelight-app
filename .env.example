# Vercel AI Gateway key used by the jelight/gateway adapter.
AI_GATEWAY_API_KEY=

# Optional: override the model id (defaults to typesafe-ai/jev).
# AI_MODEL=typesafe-ai/jev

# Optional: override the HTTP port (defaults to 3000).
# PORT=3000
